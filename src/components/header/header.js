import React from 'react';
import {Text, View} from 'react-native';
import styles from './header.style';

const Header = () => {
  return <Text style={styles.headText}>toDO</Text>;
};

export default Header;
